//TODO: create an filter based in cache strat
const INITIAL_STATE = {
	cache: {
		pages: []
	},
}

export default (state = INITIAL_STATE, action) => {
	switch (action.type) {
		default:
			return state
	}
}
